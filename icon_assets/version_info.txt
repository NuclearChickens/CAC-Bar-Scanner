# UTF-8
#
# PyInstaller VS_VERSION_INFO file. Drives the metadata shown when a
# user right-clicks BarScanner.exe → Properties → Details.
#
# Keep the values here in sync with version.py — version.py is the
# source of truth, this file mirrors it. If you change one, change the
# other and rebuild.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName',      u'Jeremy Evans'),
         StringStruct(u'FileDescription',  u'CAC Bar Scanner — barcode access control'),
         StringStruct(u'FileVersion',      u'1.0.0.0'),
         StringStruct(u'InternalName',     u'BarScanner'),
         StringStruct(u'LegalCopyright',   u'Copyright (c) 2026 Jeremy Evans'),
         StringStruct(u'OriginalFilename', u'BarScanner.exe'),
         StringStruct(u'ProductName',      u'CAC Bar Scanner'),
         StringStruct(u'ProductVersion',   u'1.0.0.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
